services:
  db:
    image: mariadb:11
    environment:
      MARIADB_DATABASE: ${MARIADB_DATABASE:-boom}
      MARIADB_USER: ${MARIADB_USER:-boom}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD:-boom}
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-boomroot}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 3s
      retries: 20

  ollama:
    image: ollama/ollama:0.18.1
    volumes:
      - ollama_data:/root/.ollama
      - models_data:/models
    # Host 11435 avoids conflict with another Ollama (or container) on 11434; inside the stack it's still ollama:11434.
    ports:
      - "11435:11434"
    tty: true
    stdin_open: true
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/app
      - models_data:/models
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      DATABASE_URL: mysql+pymysql://${MARIADB_USER:-boom}:${MARIADB_PASSWORD:-boom}@db:3306/${MARIADB_DATABASE:-boom}
      JWT_SECRET: ${JWT_SECRET:-change-me}
      HF_TOKEN: ${HF_TOKEN:-}
      # Python downloader reports progress to DB; Rust hf_transfer often does not
      HF_HUB_ENABLE_HF_TRANSFER: "0"
      MODELS_DIR: /models
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
      OLLAMA_HOST: http://ollama:11434
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      ollama:
        condition: service_started

  frontend:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    environment:
      VITE_API_BASE_URL: ""
      VITE_PROXY_TARGET: http://backend:8000
      CHOKIDAR_USEPOLLING: "true"
    command: sh -lc "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  db_data:
  models_data:
  ollama_data:
